# Current State

**Phase:** Clean Gameplay Milestone 1 implemented; validation/export pending.

The project now contains a minimal Godot portrait board with a horizontal launcher, top/side containment, a visual-only danger line, a two-level current/next queue, and isolated contact-only gem merging. The table has no pre-placed gems; the only initial piece is the launcher below the line.

The next milestone must not alter merge behavior without a focused request and regression tests. Chain merges, score, loss/win, and all polish remain out of scope.