# Responsive scene variety evidence

These six PNGs are real Godot 4.6.3 Compatibility/ANGLE renders from production `Game.tscn`, captured by `tests/capture_responsive_scene_variety.gd`.

- 720x1280 and 720x1600 are each represented by levels 1, 7, and 19.
- Filenames record the deterministic level, background, and table indices.
- The set verifies the enlarged edge cards, vertical Next/Settings order, centered Target/path stack, table breathing room, background cover behavior, and common table/physics alignment across three distinct art pairings.
